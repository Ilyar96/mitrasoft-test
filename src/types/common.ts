export enum FetchingStatus {
	"IDLE" = "idle",
	"PENDING" = "pending",
	"SUCCESS" = "success",
	"FAILURE" = "failure",
}

export enum AppRoutes {
	"HOME" = "/",
	"USERS" = "/users",
	"ABOUT" = "/about",
}

export type Id = number;
